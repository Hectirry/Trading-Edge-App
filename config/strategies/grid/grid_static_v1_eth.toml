# grid_static_v1 — ETH/USDT fixed-range static grid (Phase 3.8a).
#
# Baseline variant for ETH. Params mirror BTC geometry but scaled to ETH
# price (~$2500 at time of plan, step ~0.8% = $20).

name = "grid_static_v1_eth"
description = "Fixed-range static grid, ETH/USDT spot Binance. 3.8a smoke."

strategy_id = "grid_static_v1_eth"
instrument_id = "ETHUSDT.BINANCE"

[params]
step = 20.0
geometric = false
n_below = 15
n_above = 15
qty_per_level = 0.02
stop_loss_pct = 0.15

[paper]
capital_usd = 1000.0
maker_fee_bps = 10.0              # [PROVISIONAL] Binance spot 0.1%
